#include "speedtest.h"

#include <curl/curl.h>
#include <stdlib.h>
#include <string.h>

int get_download_speed(Server *servers, size_t count) {
    return EXIT_SUCCESS;
}

int get_upload_speed(Server *servers, size_t count) {
    return EXIT_SUCCESS;
}